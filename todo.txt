to test locally using static data
---------------------------------
 % cd mock-api; npm install
 % cd interface; npm run standalone

files changed in interface
--------------------------
 .env - changed project names
 config-overrides.js - added conditional for hosted (https://ems-esp.derbyshire.nl/)
 package.json - added new scripts, changed proxy, added dev, added "@msgpack/msgpack" and some other things
 
 public/index.html - my own project name
 public/app/manifest.json - my own project name
 public/app/icon.png - my own icon
 public/favicon.ico - my own icon

 src/CustomTheme.tsx (mode: dark) added background, made font size smaller to 13

 src/api/env.ts - changed default project name
 src/api/endpoints.ts - added a AXIOS_API for /api and also AXIOS_BIN using msgpack decoding, also ES
 src/api/Env.ts - add Event Source
 
 src/components/layout/Layout.tsx - changed DRAWER_WIDTH to 230
 src/components/layout/LayoutAppBar.tsx - changed DRAWER_WIDTH to 230, set backgroundColor and color to the sx
 src/components/layout/LayoutMenuItem.tsx - changed color of icons to grey

 src/components/ButtonRow.tsx - added top padding (mt) to give space
 src/components/SectionContext.tsx - added divider with title in center. Added a new props 'id' needed in SystemLog.tsx
 src/components/loading/FormLoader.tsx - remove the word "Loading..."

 src/validators/network.ts - added the extra fields
 src/validators/shared.ts - added validator for IPv6

 src/setupProxy.hs - added Event Source

 src/Project/ProjectRouting.tsx - had to add fixed and wildcards for nav to work
 src/AppRouting.tsx - renamed 'log in' to 'sign in' to be consistent

 * changed all <Button> variant="contained" to variant="outlined"
 * removed pt={2} from all ButtonRow's and added it to the actual ButtonRow component

new files added in interface
----------------------------
 .env.hosted
 .env.production
 .env.development was removed
 .prettierrc

customized the features
-----------------------
 - MQTT - additional fields for my EMS-ESP project
 - Wifi/Network - renamed wifi to network everywhere, including the api endpoints to allow for Ethernet
 - System - added system log and version check. plus other params like uptime.

still to do
-----------

Platform/Interface related:
 - Question: TextField validating numbers when empty giving warning "The specified value "NaN" cannot be parsed, or is out of range."
 - Question: AccessPoint - need to add referred Channel, Hide SSID options & Max Clients to backend framework library
 - Question: build is bigger than pre-hooks. Is that expected, even with out all the fonts?
 - Question: why using react-router-dom v5.3 instead of v6?
 - Question: GitHub complains about security vulnerabilities from create-react-app. Can be fixed by https://www.npmjs.com/package/npm-force-resolutions. See https://github.com/facebook/create-react-app/issues/10411#issuecomment-948600254

 - Enhancement: Can the Upload form refreshed itself automatically after a successful upload?
 - Enhancement: Web Socket ping back to the server (ESP32) to see if it's still alive and try and reconnect
 - Enhancement: Show flashing up of "Loading..." only if it takes too long to refresh?
 - Enhancement: Create an useEs.ts for Event Source (I use it in my System Log)

EMS-ESP project things to finish
--------------------------------
- add ethernet params to the EMS-ESP settings, change phy_type
- reload sensor in Dashboard after rename, same with thermostat
- fix big when system log crashes with too many messages in eventlog array
- system log - find out how to make the Box autoscroll
